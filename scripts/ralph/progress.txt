# Ralph Progress Log
Started: Fri Mar 27 19:21:05 +03 2026
---

## 2026-03-27 - US-001
- Added `listExecutions`, `findStepOutput`, `deleteExecutionsBefore` to `ExecutionRepository`
- Files changed: `apps/platform/automation/src/repositories/execution.repository.ts`, `apps/platform/automation/test/unit/execution.repository.test.ts`
- **Learnings for future iterations:**
  - `listExecutions` uses two queries (paginated executions + steps by IDs) then merges in JS — correct pagination without LIMIT distortion from JOIN
  - For mocking two sequential `db()` calls, use a `callCount` counter in `vi.fn().mockImplementation()` to return different query builders per call
  - Making a Knex mock thenable for `await query` requires defining a `then` getter that resolves immediately
  - `deleteExecutionsBefore` uses a subquery passed to `whereIn` — Knex handles this as a SQL subquery automatically
---
