## 2026-03-30 - US-003
- What was implemented: NotificationsRepo class with full DB access layer
- Files changed:
  - apps/platform/notification/src/repositories/notifications.repo.ts (new file)
- **Learnings for future iterations:**
  - `andOnVal('column', value)` in Knex joinClause correctly binds a value to a JOIN ON condition — used for per-user LEFT JOIN on notification_reads
  - Cursor pagination: encode `{created_at, id}` as base64 JSON; decode and apply `(created_at < cursor OR (created_at = cursor AND id < cursor_id))` for DESC order
  - `totalUnread` is a separate COUNT query (no limit/before/unread filter) run in parallel with the main query
  - `findMissed` adds `read: false` to rows since missed notifications by definition are unread from the client's perspective
  - `onConflict(['user_id', 'notification_id']).ignore()` for idempotent upserts in notification_reads
---

## 2026-03-30 - US-004
- What was implemented: Channel validator service with pattern enforcement and JWT-based access control
- Files changed:
  - apps/platform/notification/src/services/channel-validator.ts (new file)
  - apps/platform/notification/test/unit/channel-validator.test.ts (new file, 17 tests)
- **Learnings for future iterations:**
  - Accepted patterns: `location:{id}:{type}`, `user:{id}:{type}`, `global:system` — all others rejected
  - Empty segment detection: split on `:` and check `parts.some(p => p === '')` catches trailing colons, double colons, etc.
  - `global:system` is exact-match only — `global:other` and `global:system:extra` are both invalid
  - `validateChannelAccess` checks `locations` array for `location:*`, `sub` equality for `user:*`, and always returns true for `global:*`
---

## Codebase Patterns
- Platform services use ESM (`"type": "module"`) with NodeNext module resolution — always use `.js` extensions in imports
- tsconfig follows automation service pattern: target ES2022, NodeNext, strict, outDir dist/, rootDir src/
- Fastify app is exported from index.ts; server only starts when NODE_ENV !== 'test' (allows testing without binding port)
- config.ts uses requireEnv() helper that throws on missing required vars; PORT has default 3006
- BullMQ and ioredis are separate dependencies (ioredis used for pub/sub, BullMQ for job queues)
- Services run on port 3006 (notification), matches existing port allocation pattern

---

# Ralph Progress Log
Started: Mon Mar 30 13:08:41 +03 2026
---

## 2026-03-30 - US-002
- What was implemented: Database migrations for platform_notifications schema
- Files changed:
  - apps/platform/notification/migrations/001_create_notifications.ts (notifications + notification_reads tables with all required indexes)
  - apps/platform/notification/migrations/knexfile.ts (pg client, platform_notifications searchPath)
- **Learnings for future iterations:**
  - Use `table.specificType('seq', 'bigserial').notNullable()` for bigserial non-PK column in Knex
  - Descending and partial indexes must be created with `knex.raw()` — Knex doesn't support them natively
  - FK reference for cross-table in same schema: use `.references('id').inTable('platform_notifications.notifications')` (full schema-qualified)
  - knexfile.ts mirrors automation service pattern: searchPath includes schema + public
---

## 2026-03-30 - US-001
- What was implemented: Full project scaffold for @platform/notification service
- Files changed:
  - apps/platform/notification/package.json (name: @platform/notification, all required deps)
  - apps/platform/notification/tsconfig.json (ESM, strict, NodeNext, dist/ output)
  - apps/platform/notification/src/config.ts (requireEnv helper, PORT default 3006)
  - apps/platform/notification/src/index.ts (Fastify app, GET /health, exports app)
  - apps/platform/notification/Dockerfile (node:24-alpine, npm ci --omit=dev, dist/)
  - apps/platform/notification/.env.example (all 4 env vars)
  - apps/platform/notification/.gitignore
  - apps/platform/notification/package-lock.json
- **Learnings for future iterations:**
  - All other platform services (messaging, email, etc.) directories are empty placeholders - notification is the first to be built
  - Used automation service as reference for package.json/tsconfig patterns
  - config.ts throws at module load time if required env vars are missing - this means tests need env vars set (or NODE_ENV=test guard in index.ts)
  - Typecheck passes with no issues on the scaffold
---
