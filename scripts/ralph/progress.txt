# Ralph Progress Log
Started: Fri Mar 27 18:13:31 +03 2026
---

## Codebase Patterns
- Use `const SCHEMA = 'platform_automation'` + template literals for table names
- Knex bulk insert: `this.db(TABLE).insert(rowsArray)` — no `.returning()` needed when return value is void
- Worker pattern: `createXxxProcessor(repo, queue, fetchFn?)` returning `(job: Job<ActionJobData>) => Promise<void>`
- Active hours check occurs BEFORE `updateStepStatus('running')` — if delaying, step stays 'pending' until window opens
- Use `job.moveToDelayed(Date.now() + delay, job.token)` to delay a BullMQ job from inside the processor
- `resolveParams(action_params, event, exec_ctx)` handles all field interpolation; routing keys (`_next_step_id`, etc.) survive as literal UUIDs
- `send_message`/`send_email` construct HTTP payload manually from extracted fields — no need to strip routing keys from body
- Use `process.env[ENV_CONSTANT]` bracket notation (with exported constant) so tests can stub via `process.env`
- Inject `fetchFn = globalThis.fetch` as last parameter to all workers that make HTTP calls — enables clean test mocking
- Branch worker: does NOT call `updateExecutionStatus` — responsibility of the final action worker in the chain
- `active_hours` flows through the entire action chain via `ActionJobData` — all workers must propagate it when enqueuing next steps
- `JobCanceller.cancelDelayedJobsForRule(ruleId)` uses `queue.getDelayed()` + filter by `job.data.exec_ctx.rule_id`

## 2026-03-27T18:16 - US-001
- Added `active_hours?: ActiveHoursConfig | null` to `ActionJobData` interface in `src/queue/index.ts`
- Added `import type { ActiveHoursConfig }` to `execution-manager.ts`; propagates `rule.rule.active_hours ?? null` in root job enqueue
- Updated `branch.worker.ts`, `enroll-sequence.worker.ts`, `emit-event.worker.ts` to destructure and forward `active_hours` in their `queue.add(...)` calls
- Updated 4 test files to include `active_hours` in job fixtures; used a positive value `{ start: '08:00', end: '20:00', timezone_field: 'payload.tz' }` in each worker's next-step enqueue test to prove forwarding (not hardcoded)
- Files changed: `src/queue/index.ts`, `src/services/execution-manager.ts`, `src/services/action-workers/{branch,enroll-sequence,emit-event}.worker.ts`, `test/unit/execution-manager.test.ts`, `test/unit/action-workers/{branch,enroll-sequence,emit-event}-worker.test.ts`
- **Learnings for future iterations:**
  - All `queue.add(...)` call sites need `active_hours` — always grep for them when adding new job data fields
  - The `makeJob()` fixture in tests defaults `active_hours: null`; override with a real config in the next-step assertion test to prove forwarding
  - `rule.rule.active_hours` is typed as `unknown` on `MatchedRule['rule']` — cast to `ActiveHoursConfig | null` and use `?? null`
---

## 2026-03-27T18:19 - US-002
- Created `src/services/action-workers/send-message.worker.ts` with active hours gating (before marking 'running'), Template Service render, Messaging Service send, and next-step chaining
- Created `test/unit/action-workers/send-message-worker.test.ts` with 10 tests covering all acceptance criteria
- Files changed: `src/services/action-workers/send-message.worker.ts`, `test/unit/action-workers/send-message-worker.test.ts`
- **Learnings for future iterations:**
  - `vi.mock` on `active-hours.js` must come before the import of `computeDelay` — vitest hoists `vi.mock` calls automatically so ordering in file doesn't matter, but `.js` extension is required in the mock path
  - To mock `job.moveToDelayed`, add it directly to the mock job object returned by `makeJob()` — BullMQ's `Job` class doesn't need to be imported for this
  - `makeSuccessFetch()` helper using `mockResolvedValueOnce` chains is the cleanest pattern for workers with two sequential fetch calls
  - Active hours check uses `active_hours !== null && active_hours !== undefined` — handles both explicit null and missing field
  - `send_message` extracts fields from resolved params individually (not destructuring from spread) so routing keys like `_next_step_id` stay out of HTTP bodies
---

## 2026-03-27T18:21 - US-003
- Created `src/services/action-workers/send-email.worker.ts` with same active hours gating, two-step render-then-send pattern as send_message, but using Email Service (`/emails/send`) and three-field template response (`subject`, `body_html`, `body_text`); no `from_field` (Email Service manages sender)
- Created `test/unit/action-workers/send-email-worker.test.ts` with 10 tests covering all acceptance criteria (mirrors send-message test structure)
- Files changed: `src/services/action-workers/send-email.worker.ts`, `test/unit/action-workers/send-email-worker.test.ts`
- **Learnings for future iterations:**
  - send_email differs from send_message in 3 ways: no `from_field`, template response has 3 fields (`subject`, `body_html`, `body_text`), endpoint is `/emails/send`
  - `TEMPLATE_SERVICE_URL_ENV` is re-exported from send-email.worker.ts as its own constant (not re-imported from send-message.worker.ts) — duplication is intentional per spec
  - Test helper `makeSuccessFetch()` must return all three template fields (`subject`, `body_html`, `body_text`) or the destructuring will yield `undefined` values
---

## 2026-03-27T18:25 - US-004
- Created `src/services/job-canceller.ts` — `JobCanceller` class with `cancelDelayedJobsForRule(ruleId)` using `queue.getDelayed()` + filter by `exec_ctx.rule_id`
- Modified `src/routes/rules.ts`: added `JobCanceller` import, extended plugin options type to `{ db: Knex; jobCanceller?: JobCanceller }`, added `opts.jobCanceller?.cancelDelayedJobsForRule(id)` in PATCH status (when disabled) and DELETE handlers
- Modified `src/index.ts`: moved `connection`/`queue`/`jobCanceller` creation before `fastify.register(rulesRoutes, ...)`, added `send_message` and `send_email` workers to the workers array
- Created `test/unit/job-canceller.test.ts` with 3 tests: matching jobs cancelled + count returned, no matching jobs, empty queue
- Files changed: `src/services/job-canceller.ts` (new), `src/routes/rules.ts`, `src/index.ts`, `test/unit/job-canceller.test.ts` (new)
- **Learnings for future iterations:**
  - In index.ts, queue/jobCanceller must be created BEFORE `fastify.register(rulesRoutes, ...)` to pass jobCanceller at register time — but workers can still be created after `fastify.listen()`
  - Optional chaining `opts.jobCanceller?.cancelDelayedJobsForRule(id)` keeps backward compat for tests that don't pass a jobCanceller
  - `queue.getDelayed()` returns full Job objects with `.data` and `.remove()` — no need to import the Job class in tests, just mock the shape
---
