# Ralph Progress Log
Started: Sun Mar 29 22:36:39 +03 2026
---

## 2026-03-29 - US-006
- Created `src/workers/campaign-recipient-worker.ts` — BullMQ worker (concurrency: 10) on queue `campaign-recipient`
- Worker steps: fetch recipient (crash recovery guard), increment attempt, fetch job + domain, render template (TemplateRenderError → markFailed; TemplateServiceUnavailableError → throw/retry), resolve subject (simple `{{var}}` regex replace), call SendGrid, handle 202/400/other
- Completion check helper (inline): re-fetches job counts after increment, computes `failed`/`completed_with_errors`/`completed`, calls `attemptCompletion` (atomic), publishes `email.campaign_completed` on win
- SendGrid 400 (bounce): calls `markBounced`, publishes `email.bounced` with `to_address` field
- Updated `src/index.ts`: imports `createCampaignRecipientWorker`, creates worker after `app.listen`, adds `campaignWorker.close()` to SIGTERM handler
- Created `src/workers/campaign-recipient-worker.test.ts` — 8 test cases covering all scenarios
- Files changed: `src/workers/campaign-recipient-worker.ts`, `src/workers/campaign-recipient-worker.test.ts`, `src/index.ts`
- **Learnings for future iterations:**
  - `checkCompletion` re-fetches job AFTER `attemptCompletion` to get final counts for event payload (counts at completion time)
  - Subject template `{{varName}}` replacement uses `/\{\{(\w+)\}\}/g` regex — simple string replace, no lib
  - In test mocks for worker: `vi.mocked(Worker).mockImplementation((_queue, proc) => ...)` — only 2 args captured (queue name and processor); options are the 3rd arg, not needed for tests
  - `TemplateRenderError` and `TemplateServiceUnavailableError` must be defined inline in the `vi.mock` factory (same hoisting issue as before)
  - `mockJobsRepo.findById` is called multiple times in one test (first for the job, then for completion check counts) — default mock should return the post-increment state
---

## 2026-03-29 - US-005
- Added GET /campaigns/:jobId, GET /campaigns/:jobId/recipients, DELETE /campaigns/:jobId to `campaigns.ts`
- Extended `campaigns.test.ts` with a second describe block covering 8 new scenarios (GET 200/404, GET recipients paginated/filtered, DELETE pending/processing/completed/unknown)
- Files changed: `src/routes/campaigns.ts`, `src/routes/campaigns.test.ts`
- **Learnings for future iterations:**
  - Multiple describe blocks sharing the same vi.mock declarations work fine — mocks are hoisted once per file; each describe sets up its own mockImplementation in beforeEach
  - GET recipients: must check job existence first (404) before calling findByJobIdPaginated
  - DELETE cancel guard: only `['pending', 'spam_check_failed']` statuses are cancellable; all others → 409
---

## Codebase Patterns
- TypeBox `maxItems` on arrays returns 400 (schema validation), not 422 — for custom 422 limit errors, omit maxItems and check manually in the handler
- vi.mock factory cannot reference top-level imports (hoisting issue); define error classes inline inside the factory function when mocking modules that export both a class and error classes
- spamscanner (npm) uses `scanner.scan(rfcEmailString)` not `scanner.getReport()` — build a minimal RFC 2822 string from subject/html/text; map `classification.probability * 10` to a 0–10 score
- spamscanner ships its own TypeScript types at `dist/types/index.d.ts`; no extra @types package needed
- Repositories follow TypeBox schema + Static type pattern: define `*Schema` with `Type.Object(...)`, export `type * = Static<typeof *Schema>`, then export the class
- Repository constructors take `private readonly db: Knex`; all queries use `this.db('table_name')`
- Nullable columns use `Type.Union([Type.String(), Type.Null()])` in TypeBox schemas
- `returning('*')` + cast `row as T` for insert operations
- Use `this.db.fn.now()` for timestamp fields in updates
- `app.decorate()` adds typed fields to FastifyInstance; corresponding module augmentation lives in `types.d.ts`
- Routes registered via `app.register(plugin, { prefix: '/emails' })` in `buildApp()`
- ioredis: use `import { Redis } from 'ioredis'` (named export) — default import `IORedis` cannot be used as a type
- BullMQ requires `maxRetriesPerRequest: null` on the IORedis connection
- When `buildApp` signature changes, update `app.test.ts` stubs (all 3 call sites)
- BullMQ worker tests: use top-level `vi.mock('bullmq', () => ({ Worker: vi.fn() }))`, capture processor + failed handler in `beforeEach` by setting `Worker.mockImplementation` before calling `createWorker()`
- Worker processor is tested by calling `capturedProcessor({ data: jobData })` directly — no real BullMQ queue needed
- Type the `publishSpy` as `any` when using `vi.spyOn` on an EventBus method to avoid complex `MockInstance` variance errors

---

## 2026-03-29 - US-004
- Created `TemplateServiceClient` in `src/clients/template-service-client.ts` — `TemplateRenderError` (4xx) and `TemplateServiceUnavailableError` (5xx/timeout)
- Added required `TEMPLATE_SERVICE_URL` env var to `src/env.ts` and `vitest.config.ts`
- Created `campaignRoutes` in `src/routes/campaigns.ts` — POST /emails/campaigns/send with 9-step flow (domain check, dedup, limit, create, template render, spam check, bulk insert, set processing, enqueue)
- Created full test suite in `src/routes/campaigns.test.ts` (9 scenarios)
- Registered `campaignRoutes` in `buildApp()` with prefix `/emails`
- Files changed: `src/clients/template-service-client.ts`, `src/routes/campaigns.ts`, `src/routes/campaigns.test.ts`, `src/env.ts`, `src/app.ts`, `vitest.config.ts`
- **Learnings for future iterations:**
  - TypeBox `maxItems` returns 400, not 422 — remove it and check manually in the handler
  - vi.mock factory cannot reference top-level imports (hoisted before init); define error classes inline inside the factory; import them via dynamic `await import(...)` in beforeEach for use in test throw sites
  - `findPendingByJobId` used post-bulkInsert to get recipient IDs for queue.add (since bulkInsert has no returning)
  - scheduled_for delay: `Date.parse(scheduled_for) - Date.now()` — use 0 if in past or absent
---

## 2026-03-29 - US-003
- Implemented `SpamCheckerService` using the real spamscanner `scan()` API (builds RFC 2822 string from subject/html/text, maps classification.probability×10 to score)
- Added `SPAM_SCORE_THRESHOLD_DEFAULT` optional number env var to `env.ts`
- Created `routes/spam-check.ts` — POST /emails/spam-check, mocks SpamCheckerService in tests
- Registered `spamCheckRoutes` in `buildApp()` with prefix `/emails`
- Files changed: `src/env.ts`, `src/app.ts`, `src/services/spam-checker.ts`, `src/routes/spam-check.ts`, `src/routes/spam-check.test.ts`, `package.json`, `package-lock.json`
- **Learnings for future iterations:**
  - spamscanner ships its own TypeScript types; import as `import SpamScanner from 'spamscanner'`
  - spamscanner API: `scanner.scan(rfcString)` — not `getReport()`. The spec predates the current API version
  - vitest.config.ts already sets all required env vars; `SPAM_SCORE_THRESHOLD_DEFAULT` is optional so no config change needed
  - Route test pattern: `vi.mock('../services/spam-checker.js', () => ({ SpamCheckerService: vi.fn() }))` then set `.mockImplementation(() => mockInstance)` in beforeEach
---

## 2026-03-29 - US-002
- Added `campaignRecipient` BullMQ Queue to `createQueues()`, `FastifyInstance` types, `buildApp()` onClose hook
- Updated `sends.test.ts` stub to include `campaignRecipient` (was missing — caused typecheck failure)
- Files changed: `apps/platform/email/src/queue.ts`, `src/types.d.ts`, `src/app.ts`, `src/app.test.ts`, `src/routes/sends.test.ts`
- **Learnings for future iterations:**
  - When extending `buildApp` queues type, ALL existing test files that call `buildApp` need their stubs updated — check `sends.test.ts` in addition to `app.test.ts`
---

## 2026-03-29 - US-001
- Implemented `EmailCampaignJobsRepository` with full CRUD, atomic increment methods, and `attemptCompletion` via raw SQL RETURNING
- Implemented `EmailCampaignRecipientsRepository` with bulk insert, paginated query (parallel count), and status update methods
- Files changed: `apps/platform/email/src/repositories/email-campaign-jobs-repository.ts`, `apps/platform/email/src/repositories/email-campaign-recipients-repository.ts`
- **Learnings for future iterations:**
  - `attemptCompletion` uses `db.raw(...)` with positional `?` params and checks `result.rows.length > 0` for the RETURNING guard
  - `findByJobIdPaginated` runs `query.select().limit().offset()` and `countQuery.count().first()` in `Promise.all` — both branches must be built from the same base query before either is executed
  - `Type.Integer()` used for integer columns (total_recipients, sent_count, etc.) — consistent with spec language
---
