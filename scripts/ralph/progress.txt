## Codebase Patterns
- Test files in `test/unit/` import from `../../src/services/<module>.js` (ESM `.js` extension required even for `.ts` sources)
- The project uses `"type": "module"` + NodeNext resolution — always use `.js` extensions in imports
- `vitest run` executes all test files under `test/`; no extra config needed
- Pure service modules go in `src/services/`; no registration in index.ts needed for pure functions
- DOT_PATH_RE must be checked before TEMPLATE_TOKEN_RE since dot-paths won't contain `{{}}` but template strings might match path-like patterns

# Ralph Progress Log
Started: Fri Mar 27 16:08:49 +03 2026
---

## 2026-03-27 - US-001
- Implemented `src/services/field-interpolator.ts` with `ExecutionContext` type, `resolveValue`, and `resolveParams`
- Implemented `test/unit/field-interpolator.test.ts` with 15 tests covering all acceptance criteria
- Files changed: `src/services/field-interpolator.ts`, `test/unit/field-interpolator.test.ts`
- **Learnings for future iterations:**
  - The `TEMPLATE_TOKEN_RE` regex has state (`lastIndex`) when using `g` flag — must reset with `TEMPLATE_TOKEN_RE.lastIndex = 0` before calling `.test()` and then `.replace()`, or use a fresh regex literal in `.replace()`
  - Test data must be carefully aligned: `'literal'` matches the dot-path regex `/^[a-zA-Z_][\w.]*$/` so use strings with spaces or hyphens for actual string literals in tests
  - `source` is a top-level key in `eventCtx`; `payload.source` would be undefined — match your test event shape carefully
  - All 20 tests pass (5 rule-validator + 15 field-interpolator)
---
